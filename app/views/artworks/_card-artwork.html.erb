 <div class="card">
  <%= cl_image_tag artwork.photo %>
  <div class="card-trip-infos">
    <div>
      <p class="artwork_title"><strong><%= artwork.title.capitalize %></strong></p>
      <p> <%= artwork.artist.name.capitalize %></p>
    </div>
  </div>
</div>
